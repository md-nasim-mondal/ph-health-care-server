//? user related enum 
enum UserRole {
    PATIENT
    DOCTOR
    ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

enum Gender {
    MALE
    FEMALE
}

//? Appointment Related Enum

enum AppointmentStatus {
    SCHEDULED
    IN_PROGRESS
    COMPLETED
    CANCEL
}

enum PaymentStatus {
    PAID
    UNPAID
}
